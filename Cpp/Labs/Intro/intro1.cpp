// one of each type
// char, int, double, std::string

// not allowed to use if, while, for.

// only sequential instructions.

#include <iostream>
#include <iomanip>

//using namespace std;
using std::cout, std::cin, std::endl, std::setprecision, std::setfill, std::setw, std::string;


// Example 1

int main() {

    /*
    int a{};

    cout << "Enter one intger: ";
    cin >> a;
    cout << "You entered the number: " << a << "\n" << endl;

    

    int a1{};
    int a2{};
    int a3{};
    int a4{};

    cout << "Enter four integers: ";
    cin >> a1 >> a2 >> a3 >> a4;
    cout << "You entered the numbers: ";
    cout << a1 << " ";
    cout << a2 << " ";
    cout << a3 << " ";
    cout << a4 << "\n" << endl;

    

    int i{};
    double d{};

    cout << "Enter one integer and one real number: ";
    cin >> i >> d;
    cout << "The real is: \t\t" << setprecision(4) << d << "\nThe integer is: \t" << i << "\n" << endl;

    

    int i{};
    double d{};

    cout << "Enter one real and one integer number: ";
    cin >> d >> i;
    cout << "The real is: ";
    cout << setw(6) << setfill('.') << "";
    cout << setprecision(4) << d;
    cout << "\nThe integer is: " << setw(4) << setfill('.') << ""; 
    cout << i << "\n" << endl;

    

    char c{};

    cout << "Enter a character: ";
    cin >> c;
    cout << "You entered: " << c << "\n";

    

    string s{};
    cout << "Enter a word: ";
    cin >> s;
    cout << "The world '" << s << "' has " << s.length() << " character(s)\n" << endl;

    

    int si{};
    string s1;
    cout << "Enter an integer and a word: ";
    cin >> si >> s1;
    cout << "You entered '" << si << "' and '" << s1 << "'.\n" << endl;

    

    char c1{};
    char c2[100];
    cout << "Enter a character and a word: ";
    cin >> c1 >> c2;
    cout << "You entered the string \"" << c2 << "\" and the character '" << c1 << "'.\n" << endl;

    

    // skip

    

    string s2;
    cout << "Enter a text-line: ";
    getline(cin, s2);
    cout << "You entered: \"" << s2 << "\"\n" << endl;

    

    string s3, s4, s5;
    cout << "Enter three words: ";
    cin >> s3 >> s4 >> s5;
    cout << "You entered: '" << s3 << " " << s4 << " " << s5 << "'\n" << endl;

    */

    // ----------------------------------- Exmaple 2 -------------------------------

    /*

    int i3{};
    cout << "Enter one integer: ";
    cin >> i3;      // integer will always convert/cut any excess decimals.
    cout << "You entered the number: " << i3;

    

    int i4, i5, i6, i7;
    cout << "Enter four integers: ";
    cin >> i4 >> i5 >> i6 >> i7;
    cout << "You entered the numbers: " << i4 << " " << i5 << " " << i6 << " " << i7 << "\n" << endl;

    

    // skipp rest.

    */

    return 0;

}