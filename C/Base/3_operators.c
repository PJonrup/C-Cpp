

int main (void) {
    
    int a = 2;
    int b = 5;

    /* 
    
    -------------------- Arithmetic operators --------------------

    Binar operators:
    a = b
    a + b
    a - b
    a * b
    a / b
    a % b

    
    Unary operators:
    +a
    -a
    a++     - increments a after using it
    ++a     - increments a before using it
    a--, --a

    
    -------------------- Comparison operators --------------------
    a == b
    a != b
    a > b, a < b
    a >= b, a <= b

    
    -------------------- Logical operators --------------------
    !a
    a && b
    a || b

    
    -------------------- Compound assignment operators --------------------
    a += b
    a -= b
    a *= b
    a /= b
    a %= b

   
    -------------------- Ternary operation --------------------
    <condition> ? <expression> : <expression>
        bool    ?     true          false?

    
    -------------------- Operator precedence --------------------
    - the ability to use () in math and other expressions/operations.
    
    The order of precedence (highest->lowest):
    +, - (unary) --> *, / --> +, - (binary) --> assignemnt operator

    */

}